version: 1.0.{build}
image: Visual Studio 2017
build:
  verbosity: minimal

after_build:
- 7z a Game.zip "%APPVEYOR_BUILD_FOLDER%\Spike Project\Debug\Spike_FlanStudio.exe"
- 7z a Game.zip "%APPVEYOR_BUILD_FOLDER%\Spike Project\Resources"
- 7z a Game.zip "%APPVEYOR_BUILD_FOLDER%\Spike Project\*.dll"

artifacts:
  - path: SpikeFromAppveyor.zip
    name: ReleaseBaby
